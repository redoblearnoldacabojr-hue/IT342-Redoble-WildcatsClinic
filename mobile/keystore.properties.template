# Copy this file to mobile/keystore.properties and fill values for automated signing.
# Do NOT commit the real keystore or passwords. Instead, place secrets in CI.

storeFile=release.keystore
storePassword=your_store_password_here
keyAlias=your_key_alias_here
keyPassword=your_key_password_here
